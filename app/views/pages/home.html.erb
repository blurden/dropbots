<% if user_signed_in? %>
	<%= render 'user_home' %>
<% else %>
	<div class="hero-unit">
		<h1>Homepage</h1>
		<p>
		  Sample text for homepage here.
		</p>
		<p>
		  <%= link_to "Sign Up Now!", new_user_registration_path, class: "btn btn-large btn-primary" %>
		</p>
	</div>
<% end %>